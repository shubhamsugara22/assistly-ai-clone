"use client";	

import { Message } from "@/types/types";

function Messages({ 
	messages, 
	chatBotName 
} : {
	messages: Message[];
	chatBotName: string;
}) {
  return (
	<div>
	  
	</div>
  )
}

export default Messages
